import type { Remedy } from "../../lib/content-loader";

const remedy = {
  slug: "sleep",
  name: "Sleep",
  category: "Mind",
  description: "Sleep issues can affect overall health. Calming herbs can support natural sleep cycles and improve sleep quality.",
  relatedPlants: ["lemon-balm", "passionflower", "valerian-root", "chamomile", "ashwagandha", "hops"]
};

export default remedy;
